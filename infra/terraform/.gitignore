**/.terraform/
# .terraform.lock.hcl is intentionally NOT ignored — commit it for
# reproducible provider version resolution across machines/CI.
*.tfstate
*.tfstate.*
*.tfvars.local
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
